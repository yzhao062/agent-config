# Why prun defaults to Sonnet

Ordinary `prun` units default to Sonnet to preserve Codex capacity for required adversarial reviews and the other evidenced exceptions in `skills/prun/SKILL.md`.

Mechanical work goes to Sonnet or an existing script; an explicit user instruction or a declared project-local routing policy may override the default.
