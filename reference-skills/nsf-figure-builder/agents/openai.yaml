interface:
  display_name: "NSF Figure Builder"
  short_description: "Turn proposal text into editable figure specs and model-neutral figure prompts"
  default_prompt: "Use $nsf-figure-builder to decide what figure my intro or thrust needs, draft a figure spec and caption, and save a model-neutral prompt I can paste into an external image tool."
